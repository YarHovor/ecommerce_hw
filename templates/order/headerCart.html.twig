{# в этом шаблоне будем выводить сумму заказа


    используется как фрагмент и по этому не надо extends

    если у него шото будет в амоунте тогда будем выводить
    а если не будет, то пустота и останется
 #}

{% if order.amount %} {{ order.amount / 100 }} {% endif %}